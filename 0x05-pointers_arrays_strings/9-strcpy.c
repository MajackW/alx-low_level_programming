#include <stdio.h>
#include "main.h"
/**
 * _strcpy - copies a string
 * @dest: copy
 * @src: copy
 * Return: char address
 */
char *_strcpy(char *dest, char *src)
{
	char *p;

	p = dest;
	dest = src;

	return (p);
}
